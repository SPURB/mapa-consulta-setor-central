import { projetos, colocalizados  } from '../src/model'
import { getFiles } from '../src/view'
// import { testing, returnLayers } from '../src/presenter'
// returnLayers(projetos, 'asd')
// console.log(testing)
console.log(getFiles(1, projetos))
